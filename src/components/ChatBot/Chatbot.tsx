import React from "react";

// Diese Komponente wird nicht mehr benötigt, da der ChatBot jetzt
// direkt in InteractiveFlagButtons integriert ist.
// Wir behalten sie für Kompatibilität, falls sie woanders importiert wird.

const Chatbot: React.FC = () => {
  return null; // Keine Renderung, da die Funktionalität nach InteractiveFlagButtons verschoben wurde
};

export default Chatbot;
